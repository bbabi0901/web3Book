const {
  abi: BOOKFACTORY_ABI,
} = require('../../artifacts/contracts/BookFactory.sol/BookFactory.json');

const {
  abi: BOOK_ABI,
} = require('../../artifacts/contracts/Book.sol/Book.json');

module.exports = {
  BOOKFACTORY_ABI,
  BOOK_ABI,
};
